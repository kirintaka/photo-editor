<% if @image.image? %>
  <%= image_tag(@image.image.thumb.url, alt: 'Image') %>
<% else %>
          <div class="col d-flex justify-content-center">

          <%= form_for @image, html: { multipart: true } do |f| %>

            <div class="card border-dark" style="width: 30rem; height: 10rem;">
              <div class="card-body">
                <h5 class="card-title">Image was deleted. Upload new one.</h5>
                <p class="card-text"><%= f.file_field :image %></p>
                <%= f.submit "Upload", class: "btn btn-outline-dark" %>
              </div>
            </div>

          <% end %>

          </div>
<% end %>
